const { defineConfig } = require("cypress");

module.exports = defineConfig({
  supportFile: false, // Define supportFile como false para indicar que não há arquivo de suporte

  // Restante da configuração do Cypress...
});
